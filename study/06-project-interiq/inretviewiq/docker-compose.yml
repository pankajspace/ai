services:
  interviewiq:
    build: .
    image: interviewiq:latest
    ports:
      - "7860:7860"
    env_file:
      - .env          # copy .env.example → .env and fill in OPENAI_API_KEY
    restart: unless-stopped
